<div class="container">
  <h1>Add movie to <%= @list.name %></h1>

  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.association :movie %>
    <% f.select :movie, collection: Movie.all %>
    <%= f.input :comment %>
    <div class="buttons">
      <%= f.submit "Add movie", class: "button-bookmark" %>
      <%= link_to "Back", lists_path, class: "button-back" %>
    </div>
  <% end %>
</div>
