<div class="container">
  <h1>Add new list</h1>

  <%= simple_form_for(@list) do |f| %>
    <%= f.input :name %>
    <%= f.submit class: "btn btn-primary" %>
    <%= link_to "Back", lists_path, class: "button" %>
  <% end %>
</div>
